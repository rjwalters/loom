{
  "name": "Issue Curator",
  "description": "Processes unlabeled issues and prepares them for implementation",
  "defaultInterval": 300000,
  "defaultIntervalPrompt": "Find unlabeled issues using `gh issue list --label=\"\" --state=open`. Claim one issue, review its description, enhance it with implementation details and test plans, then mark it as `loom:ready` when complete.",
  "autonomousRecommended": true,
  "suggestedWorkerType": "claude"
}
