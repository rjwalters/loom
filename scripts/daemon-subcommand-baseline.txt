# daemon-subcommand-baseline.txt — generated by scripts/check-daemon-subcommand-versions.sh --update
#
# Every (shell file, loom-daemon subcommand) dependency that predates the
# `# requires-daemon:` convention (#8285), grandfathered. A file may DROP a
# pair freely; it may not gain a new one without declaring the minimum daemon
# version in the file itself. Do NOT hand-add a row to fit a new dependency —
# that is the ratchet slipping. Write the marker instead:
#
#   # requires-daemon: <subcommand> >= <version>   <why / which PR added it>
#   # requires-daemon: <subcommand> optional       <how it degrades>
#
# A pair the file DECLARES is deliberately absent from this ledger — the
# declaration supersedes the grandfather, and carrying both would let a
# later deletion of the marker pass unnoticed.
#
# Format: one <path><TAB><subcommand> per line.
clean.sh	clean
defaults/scripts/agent-metrics.sh	stats
defaults/scripts/agent-spawn.sh	agent-spawn
defaults/scripts/agent-wait.sh	agent-wait
defaults/scripts/build-gate.sh	dispatch-backoff
defaults/scripts/claude-wrapper.sh	retry-classify
defaults/scripts/claude-wrapper.sh	tokens
defaults/scripts/clean.sh	clean
defaults/scripts/cleanup.sh	cleanup
defaults/scripts/cli/loom-daemon-watchdog.sh	daemon-watchdog
defaults/scripts/cli/loom-help.sh	status
defaults/scripts/cli/loom-status.sh	status
defaults/scripts/probe-tokens.sh	tokens
defaults/scripts/recover-orphaned-shepherds.sh	recover-orphans
defaults/scripts/resync-installed.sh	update-gitignore
defaults/scripts/spawn-claude.sh	tokens
defaults/scripts/spawn-codex.sh	tokens
defaults/scripts/spawn-worker.sh	spawn-worker
defaults/scripts/worktree.sh	lease
scripts/check-gitignore-convergence.sh	update-gitignore
scripts/install/migrate-consumer.sh	workspace
