{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'R=$(git rev-parse --git-common-dir 2>/dev/null); R=${R:+$(cd \"$R/..\" 2>/dev/null && pwd)}; W=${R:-${CLAUDE_PROJECT_DIR:-$PWD}}; L=$W/.loom/hooks/hook-wiring.sh; [ -r \"$L\" ] && LOOM_PROJECT_ROOT=$W exec bash \"$L\" PreToolUse guard-destructive.sh; G=$W/.loom/hooks/guard-destructive.sh; [ -x \"$G\" ] && exec \"$G\"; [ -r \"$G\" ] && exec bash \"$G\"; [ -d \"$W/.loom/hooks\" ] || exit 0; echo \"[loom] BROKEN GUARD INSTALL: guard-destructive.sh is not installed in this Loom workspace ($W/.loom/hooks/guard-destructive.sh); re-run scripts/install-loom.sh.\" >&2; [ \"${LOOM_GUARD_WIRING_FAILOPEN:-0}\" = \"1\" ] && exit 0; printf %s \"{\\\"hookSpecificOutput\\\":{\\\"hookEventName\\\":\\\"PreToolUse\\\",\\\"permissionDecision\\\":\\\"deny\\\",\\\"permissionDecisionReason\\\":\\\"Loom hook guard-destructive.sh is not installed in this workspace (no .loom/hooks/hook-wiring.sh and no .loom/hooks/guard-destructive.sh), but .loom/hooks/ exists, so this workspace does carry per-repo hook copies and expects this one. A missing guard is a broken install, not an opt-out, and this session runs without permission prompts. Repair: re-run scripts/install-loom.sh (or .loom/scripts/check-guards-installed.sh), then restart Claude Code. Set LOOM_GUARD_WIRING_FAILOPEN=1 in the environment to downgrade this deny to a warning.\\\"}}\"; exit 0'"
          },
          {
            "type": "command",
            "command": "bash -c 'R=$(git rev-parse --git-common-dir 2>/dev/null); R=${R:+$(cd \"$R/..\" 2>/dev/null && pwd)}; W=${R:-${CLAUDE_PROJECT_DIR:-$PWD}}; L=$W/.loom/hooks/hook-wiring.sh; [ -r \"$L\" ] && LOOM_PROJECT_ROOT=$W exec bash \"$L\" PreToolUse guard-loom-workflow.sh; G=$W/.loom/hooks/guard-loom-workflow.sh; [ -x \"$G\" ] && exec \"$G\"; [ -r \"$G\" ] && exec bash \"$G\"; [ -d \"$W/.loom/hooks\" ] || exit 0; echo \"[loom] BROKEN GUARD INSTALL: guard-loom-workflow.sh is not installed in this Loom workspace ($W/.loom/hooks/guard-loom-workflow.sh); re-run scripts/install-loom.sh.\" >&2; [ \"${LOOM_GUARD_WIRING_FAILOPEN:-0}\" = \"1\" ] && exit 0; printf %s \"{\\\"hookSpecificOutput\\\":{\\\"hookEventName\\\":\\\"PreToolUse\\\",\\\"permissionDecision\\\":\\\"deny\\\",\\\"permissionDecisionReason\\\":\\\"Loom hook guard-loom-workflow.sh is not installed in this workspace (no .loom/hooks/hook-wiring.sh and no .loom/hooks/guard-loom-workflow.sh), but .loom/hooks/ exists, so this workspace does carry per-repo hook copies and expects this one. A missing guard is a broken install, not an opt-out, and this session runs without permission prompts. Repair: re-run scripts/install-loom.sh (or .loom/scripts/check-guards-installed.sh), then restart Claude Code. Set LOOM_GUARD_WIRING_FAILOPEN=1 in the environment to downgrade this deny to a warning.\\\"}}\"; exit 0'"
          }
        ]
      },
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'R=$(git rev-parse --git-common-dir 2>/dev/null); R=${R:+$(cd \"$R/..\" 2>/dev/null && pwd)}; W=${R:-${CLAUDE_PROJECT_DIR:-$PWD}}; L=$W/.loom/hooks/hook-wiring.sh; [ -r \"$L\" ] && LOOM_PROJECT_ROOT=$W exec bash \"$L\" PreToolUse guard-worktree-paths.sh; G=$W/.loom/hooks/guard-worktree-paths.sh; [ -x \"$G\" ] && exec \"$G\"; [ -r \"$G\" ] && exec bash \"$G\"; [ -d \"$W/.loom/hooks\" ] || exit 0; echo \"[loom] BROKEN GUARD INSTALL: guard-worktree-paths.sh is not installed in this Loom workspace ($W/.loom/hooks/guard-worktree-paths.sh); re-run scripts/install-loom.sh.\" >&2; [ \"${LOOM_GUARD_WIRING_FAILOPEN:-0}\" = \"1\" ] && exit 0; printf %s \"{\\\"hookSpecificOutput\\\":{\\\"hookEventName\\\":\\\"PreToolUse\\\",\\\"permissionDecision\\\":\\\"deny\\\",\\\"permissionDecisionReason\\\":\\\"Loom hook guard-worktree-paths.sh is not installed in this workspace (no .loom/hooks/hook-wiring.sh and no .loom/hooks/guard-worktree-paths.sh), but .loom/hooks/ exists, so this workspace does carry per-repo hook copies and expects this one. A missing guard is a broken install, not an opt-out, and this session runs without permission prompts. Repair: re-run scripts/install-loom.sh (or .loom/scripts/check-guards-installed.sh), then restart Claude Code. Set LOOM_GUARD_WIRING_FAILOPEN=1 in the environment to downgrade this deny to a warning.\\\"}}\"; exit 0'"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'R=$(git rev-parse --git-common-dir 2>/dev/null); R=${R:+$(cd \"$R/..\" 2>/dev/null && pwd)}; W=${R:-${CLAUDE_PROJECT_DIR:-$PWD}}; L=$W/.loom/hooks/hook-wiring.sh; [ -r \"$L\" ] && LOOM_PROJECT_ROOT=$W exec bash \"$L\" UserPromptSubmit skill-router.sh; G=$W/.loom/hooks/skill-router.sh; [ -x \"$G\" ] && exec \"$G\"; [ -r \"$G\" ] && exec bash \"$G\"; [ -d \"$W/.loom/hooks\" ] || exit 0; echo \"[loom] BROKEN GUARD INSTALL: skill-router.sh is not installed in this Loom workspace ($W/.loom/hooks/skill-router.sh); re-run scripts/install-loom.sh.\" >&2; exit 0'"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PROJECT_DIR}/.claude/skills/repo/hooks/session-start-handoff.sh"
          }
        ]
      },
      {
        "matcher": "resume",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PROJECT_DIR}/.claude/skills/repo/hooks/session-start-handoff.sh"
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'R=$(git rev-parse --git-common-dir 2>/dev/null); R=${R:+$(cd \"$R/..\" 2>/dev/null && pwd)}; W=${R:-${CLAUDE_PROJECT_DIR:-$PWD}}; L=$W/.loom/hooks/hook-wiring.sh; [ -r \"$L\" ] && LOOM_PROJECT_ROOT=$W exec bash \"$L\" Stop guard-background-subagents.sh; G=$W/.loom/hooks/guard-background-subagents.sh; [ -x \"$G\" ] && exec \"$G\"; [ -r \"$G\" ] && exec bash \"$G\"; [ -d \"$W/.loom/hooks\" ] || exit 0; echo \"[loom] BROKEN GUARD INSTALL: guard-background-subagents.sh is not installed in this Loom workspace ($W/.loom/hooks/guard-background-subagents.sh); re-run scripts/install-loom.sh.\" >&2; exit 0'"
          },
          {
            "type": "command",
            "command": "bash -c 'R=$(git rev-parse --git-common-dir 2>/dev/null); R=${R:+$(cd \"$R/..\" 2>/dev/null && pwd)}; W=${R:-${CLAUDE_PROJECT_DIR:-$PWD}}; L=$W/.loom/scripts/lib/locate-daemon-bin.sh; [ -r \"$L\" ] || exit 0; . \"$L\" >/dev/null 2>&1 || exit 0; B=$(loom_resolve_self_daemon_bin 2>/dev/null) || exit 0; [ -n \"$B\" ] && [ -x \"$B\" ] || exit 0; \"$B\" worktree-state stop-hook || exit 0'"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'R=$(git rev-parse --git-common-dir 2>/dev/null); R=${R:+$(cd \"$R/..\" 2>/dev/null && pwd)}; W=${R:-${CLAUDE_PROJECT_DIR:-$PWD}}; L=$W/.loom/scripts/lib/locate-daemon-bin.sh; [ -r \"$L\" ] || exit 0; . \"$L\" >/dev/null 2>&1 || exit 0; B=$(loom_resolve_self_daemon_bin 2>/dev/null) || exit 0; [ -n \"$B\" ] && [ -x \"$B\" ] || exit 0; \"$B\" worktree-state stop-hook || exit 0'"
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(gh:*)",
      "Bash(git:*)",
      "Bash(pnpm:*)",
      "Bash(npx:*)",
      "Bash(cargo:*)",
      "Bash(node:*)",
      "Bash(./scripts/**:*)",
      "Bash(./.loom/scripts/**:*)",
      "Bash(tmux:*)",
      "Bash(lsof:*)",
      "Bash(cat:*)",
      "Bash(ls:*)",
      "Bash(pwd)",
      "Bash(cd:*)",
      "Bash(mkdir:*)",
      "Bash(cp:*)",
      "Bash(mv:*)",
      "Bash(rm:*)",
      "Bash(touch:*)",
      "Bash(echo:*)",
      "Bash(chmod:*)",
      "Bash(sed:*)",
      "Bash(awk:*)",
      "Bash(grep:*)",
      "Bash(find:*)",
      "Bash(xargs:*)",
      "Bash(head:*)",
      "Bash(tail:*)",
      "Bash(sort:*)",
      "Bash(uniq:*)",
      "Bash(cut:*)",
      "Bash(wc:*)",
      "Bash(tee:*)",
      "Bash(tr:*)",
      "Bash(diff:*)",
      "Bash(patch:*)",
      "Bash(which:*)",
      "Bash(env:*)",
      "Bash(basename:*)",
      "Bash(dirname:*)",
      "Bash(date:*)",
      "Bash(sleep:*)",
      "Bash(test:*)",
      "Bash([:*)",
      "Bash(jq:*)",
      "Bash(curl:*)",
      "Bash(wget:*)",
      "Bash(tar:*)",
      "Bash(gzip:*)",
      "Bash(gunzip:*)",
      "Bash(zip:*)",
      "Bash(unzip:*)",
      "Bash(convert:*)",
      "Bash(magick:*)",
      "Bash(iconutil:*)",
      "Bash(jot:*)",
      "Bash(shuf:*)",
      "WebSearch",
      "Read(/tmp/**)",
      "Read(/private/tmp/**)",
      "Edit(/tmp/**)",
      "Edit(/private/tmp/**)"
    ]
  }
}
